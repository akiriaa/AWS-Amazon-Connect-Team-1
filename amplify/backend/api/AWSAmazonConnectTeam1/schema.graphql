type Audio @model 
@auth(
  rules: [{allow: public}]
  )  {
  id: ID!
  title: String!
  description: String!
  filePath: String!
  owner: String!
}

type Chat @model   //only allow users to chat
@auth(
  rules: [{allow: private}]
) {
  id: ID!
  message: String!
  owner: String!
}